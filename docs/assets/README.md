# Repo assets (GitHub social preview)

Source: `repo-banner.svg` (1280×640).

GitHub Settings → Social preview expects **PNG/JPG** (1280×640 recommended). SVG is the editable source; export manually when updating branding:

- Inkscape / ImageMagick / browser screenshot from the SVG at 1280×640
- Do **not** claim soak or audit status in the banner — honesty strings are in the SVG

Experimental prod mesh transport: **libp2p** (Hybrid audit pin stays TCP+TLS).
