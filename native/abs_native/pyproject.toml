[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "abs-native"
version = "0.1.0"
description = "Native Rust crypto kernels for Absolute Blockchain"
requires-python = ">=3.10"

[tool.maturin]
module-name = "abs_native"
features = ["pyo3/extension-module", "libp2p"]
